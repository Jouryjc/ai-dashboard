请照着参考图复刻这样一个大屏：{{requirement}}
{{answers}}
参考图的内容清单（文字、数值、布局、配色都在里面，照抄照做）：
{{inventory}}
{{dataBlock}}
{{mapNote}}
直接输出完整 HTML。
注意：复刻模式下参考图内容清单的结构、标签、布局位置照抄。但如果上方有真实数据块，数值必须运行时用数据读取范式从 data.json 加载（与创作模式一致），不要把清单里的数值写死；没有真实数据块时才照抄清单数值并标注「示例数据」。
