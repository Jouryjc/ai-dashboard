上次生成的页面检查没通过，问题是：
{{problems}}

当前 HTML：
{{html}}

{{dataBlock}}

补丁模式没有可靠匹配到原文。请根据问题清单完整重写并修复这个 HTML。
必须保留当前页面中正确的内容、布局意图和真实数据加载逻辑；禁止把 data.json 中的真实数值写死进 HTML。
继续遵守系统消息里的尺寸、完整文档、同源数据读取和禁止外部资源约束。
只输出修复后的完整 HTML，不要解释，不要输出 JSON 补丁。
