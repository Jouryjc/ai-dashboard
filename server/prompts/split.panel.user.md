{{requirement}}

分步生成：页面骨架已经完成，现在只写「{{panelName}}」这一块面板的内部内容（图表用内联 SVG/CSS/JS 手写、深色科技风）。只输出这一块的内容片段，不要 <html>/<head>/<body> 标签，不要其他面板的内容。

数据纪律（重要）：
- 上方需求里如果带了「以下是从数据源取回的真实数据」块，这块面板的图表数值必须从里面取——按"用途"找到与本面板相关的数据条，读"数据.rows"/"数据.layers"里的真实数值写进图表，不要自己编数字。
- 只有当上方没有真实数据块、或本面板对应的数据条确实不存在时，才用示例数据，并在面板标题旁标注「示例数据」。
