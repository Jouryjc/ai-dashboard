已经确认的需求契约：

{{contract}}

当前应用蓝图；null 表示首次创建：

{{currentBlueprint}}

已脱敏的参考图呈现证据；null 表示没有参考图。它只控制应用壳、视图类型、信息层级、密度和组件角色，不能覆盖文字需求的业务语义：

{{presentationEvidence}}

合法字段结构示例。业务内容可以调整，但必须保持相同契约：

{{fallbackShape}}

请生成完整的新蓝图和本轮变更计划。
