{
  "name": "ai-dashboard",
  "version": "0.1.0",
  "description": "Loop Engineer 工作台：通过自然语言生成数据大屏或完整的 IDux 业务应用，并以可恢复循环完成验证与修复",
  "private": true,
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/Jouryjc/ai-dashboard.git"
  },
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "setup": "npm --prefix server install && npm --prefix client install",
    "dev": "bash scripts/dev.sh",
    "dev:server": "npm --prefix server run dev",
    "dev:client": "npm --prefix client run electron:dev",
    "typecheck": "npm --prefix server run typecheck && npm --prefix client run typecheck",
    "smoke": "npm --prefix server run smoke",
    "build": "npm --prefix server run build && npm --prefix client run build"
  },
  "keywords": [
    "ai",
    "dashboard",
    "data-visualization",
    "electron",
    "vue3",
    "llm"
  ]
}
