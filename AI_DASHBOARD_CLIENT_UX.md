# AI 大屏 Agent 客户端交互设计

> 供评审确认用。本文定义桌面客户端（Electron）的交互形式：信息架构、界面布局、核心流程、状态与异常处理。
> 依据：《AI_DASHBOARD_SYSTEM_DESIGN.md》第 2 节、《AI_DASHBOARD_OBSERVABILITY.md》第 5 节。
> 目标用户：不懂技术的业务人员（小白）；兼顾客服 / 交付与管理员角色。

---

## 1. 交互设计原则

1. **对话是唯一入口**：创建、修改、回退、求助都能用自然语言完成；按钮只是对话的"快捷方式"，不是必需技能。
2. **过程永远可见**：任何时候都能看到"现在进行到哪一步"，生成中 ≠ 黑盒等待（观测性设计的产品化）。
3. **一屏完成主任务**：对话、预览、执行过程同屏呈现，主流程不跳页面、不弹层叠窗口。
4. **卡点给选择，不给报错**：运行中任何异常都在对话区生成"问题处理卡片"——每次必提供选项（不超过 3 个）、必有推荐方案并说明推荐理由，用户点一下即可决策，无需打字。
5. **可逆的安全感**：每一步都有版本节点，用户知道"搞砸了也能退回去"，才敢放手让 Agent 改。

---

## 2. 信息架构

```text
桌面客户端
├── 首页（我的大屏）            ← 启动默认页
│     ├── 大屏卡片列表（封面截图 / 状态 / 最近修改）
│     └── 新建大屏入口
├── 工作台（核心，一屏式）
│     ├── 左栏：对话区
│     ├── 中区：大屏预览区
│     └── 右栏：执行过程面板（可折叠）
├── 设置中心（模态页）
│     ├── 模型设置
│     ├── 数据源
│     ├── 通知
│     └── 账户 / 关于
└── 角色视图（仅客服/管理员可见）
      ├── 协助队列
      ├── 现场回放（技术态）
      └── 模型端点管理 / 审批中心
```

---

## 3. 首页（我的大屏）

```text
┌────────────────────────────────────────────────────────────┐
│  ☰  AI 大屏工作台                     🔍 搜索   ⚙ 设置  👤 │
├────────────────────────────────────────────────────────────┤
│                                                            │
│   ┌─────────┐  ┌─────────┐  ┌─────────┐  ┌─────────┐      │
│   │ ＋ 新建  │  │ [封面图] │  │ [封面图] │  │ [封面图] │      │
│   │  大屏    │  │ K8s 监控 │  │ 销售日报 │  │ 物流追踪 │      │
│   │          │  │ ●运行中  │  │ 已发布   │  │ ⚠需处理 │      │
│   │          │  │ 2分钟前  │  │ 昨天     │  │ 3天前   │      │
│   └─────────┘  └─────────┘  └─────────┘  └─────────┘      │
│                                                            │
└────────────────────────────────────────────────────────────┘
```

交互要点：

- 卡片封面 = 最近一次构建的缩略截图，直观可认。
- 状态徽标只有四种大白话状态：`生成中` / `已完成` / `已发布` / `需要处理`（对应 BLOCKED / ESCALATED / FAILED 的聚合，点击卡片直接进入工作台并自动定位到卡点行动区）。
- 双击卡片封面 = 全屏预览大屏；单击 = 进入工作台继续编辑。

---

## 4. 工作台主界面（核心）

### 4.1 三栏布局

```text
┌──────────────────────────────────────────────────────────────────────────┐
│ ← K8s 集群监控大屏              v3 ●已保存      ⛶全屏  ↩版本  ⚙  ⋯      │
├───────────────┬──────────────────────────────────────┬───────────────────┤
│               │                                      │ ● 正在自动修复问题 │
│   对话区       │                                      │ 已进行 1 分 42 秒  │
│   320px        │          大屏预览区                   │ ───────────────── │
│               │       （内嵌 BrowserView）            │ ✓ 理解需求        │
│  ┌─────────┐  │                                      │ ✓ 查找组件        │
│  │用户消息  │  │    自适应缩放显示 1920×1080          │ ✓ 编写页面        │
│  └─────────┘  │                                      │ ● 修复问题（2/3） │
│  ┌─────────┐  │    [1920×1080 ▾] [⟳刷新]            │  ├ ✓ 图表不显示   │
│  │Agent 回复│  │                                      │  ├ ✓ 配色偏差     │
│  └─────────┘  │                                      │  └ ● 表格超出边界 │
│  ┌─────────┐  │                                      │    第 2 次尝试中… │
│  │澄清卡片  │  │                                      │ ○ 生成预览        │
│  └─────────┘  │                                      │ ───────────────── │
│               │                                      │ ▸ 查看修复细节     │
│ ─────────────│                                      │                  │
│ [输入框      │                                      │ ┌──────────────┐ │
│  发送 ▸]     │                                      │ │ 💬 遇到问题？ │ │
│ [📎][🎤]     │                                      │ │  呼叫人工协助 │ │
│               │                                      │ └──────────────┘ │
└───────────────┴──────────────────────────────────────┴───────────────────┘
                 右栏 360px，可折叠为窄条
```

### 4.2 三栏职责与交互

#### 左栏 · 对话区

- 承载全部人机交流：用户需求、Agent 回复、**澄清问答卡片**、操作结果通知。
- 消息类型五种：
  1. **用户消息**（右气泡）
  2. **Agent 消息**（左气泡，大白话）
  3. **澄清卡片**（结构化：问题 + 选项按钮 + 自定义输入，一次最多 3 个问题；每个问题如有明显更优答案，标注 ★推荐 选项，回答后立即继续，无需再发消息）
  4. **问题处理卡片**（运行中出现问题时必出现，规范见 §4.3）
  5. **系统操作条**（轻量灰条："已回退到 v2 版本"、"已为你呼叫人工"）
- 输入区：附件（📎 图片/参考稿，仅多模态模式可用，见 7.2）、语音输入（🎤，可选）、发送。
- **生成中输入框不锁定**：用户可以随时补充想法，追加的消息进入队列，Agent 当前阶段完成后合并处理（界面上提示"已收到，将在当前步骤后处理"）。这是小白用户最常见的动作——想到一句说一句，不能堵死。

#### 中区 · 大屏预览区

- 内嵌 BrowserView，固定逻辑分辨率 1920×1080，按窗口大小**等比缩放**（缩放不影响截图与视觉验证的基准）。
- 分辨率切换器：`1920×1080` / `2560×1440`（预览缩放，非真实改稿）。
- 预览的三种状态：
  - **构建中**：显示上一次可用版本 + 半透明遮罩"正在生成新版本…"（**不清空**，避免白屏焦虑）；
  - **就绪**：新版本淡入替换，右上角短暂浮现"已更新 ✓"；
  - **无任何版本**：显示占位引导（插画 + "在左侧描述你想要的大屏"）。
- 预览区支持点击组件（hover 高亮边框 + 组件名），选中后自动带入对话："**选中了「CPU 趋势图」**，想怎么调整？"——指哪改哪，小白不用学组件名称。

#### 右栏 · 执行过程面板

- 空闲态默认收起为窄条（显示当前状态一句话）；生成 / 修复时自动展开，可手动折叠。
- 内容（观测性设计 §5 的落地）：
  - **阶段时间线**：✓ 已完成 / ● 进行中（呼吸动画 + 耗时）/ ○ 未开始。
  - **Issue 卡片**：问题一句话 + 第几次尝试 + 状态；FIXED 卡片可展开看**修复前后对比截图**（左右滑块对比）。
  - **卡点行动区**（仅卡点时出现在面板顶部，高亮色）：见 §5.4。
  - 底部常驻"呼叫人工协助"按钮（非卡点时也在，降低求助门槛）。
- 「查看修复细节」展开后仍是用户态语言；技术详情入口藏在 `⋯ → 开发者视图`（默认隐藏，给客服/研发远程指导时用）。

#### 顶栏

- `←` 返回首页；标题即大屏名（点击可改名）。
- 版本指示：`v3 ●已保存` / `●生成中` / `●有未发布修改`。
- `↩版本`：打开版本时间线抽屉（见 §5.3）；`⛶全屏`：大屏全屏演示模式（隐藏三栏，投屏场景）。

### 4.3 问题处理卡片（每次必选项、必有推荐）

运行过程中**任何**需要用户知晓或决策的问题，都在对话区生成一张问题处理卡片，不允许只发一条纯文字报错，也不允许只出现在右侧面板里。

```text
┌──────────────────────────────────────────┐
│ ⚠ 自动修复没有成功                        │
│ 「表格超出边界」已经尝试了 3 次，仍没修好。 │
│                                          │
│ ┌──────────────────────────────────────┐ │
│ │ ★ 推荐  呼叫人工协助                  │ │
│ │ 支持人员能看到全部过程，通常几分钟解决 │ │
│ └──────────────────────────────────────┘ │
│ ┌──────────────────────────────────────┐ │
│ │    让 AI 再试一次（换一种方式）        │ │
│ │ 大约需要 1～2 分钟，不保证成功        │ │
│ └──────────────────────────────────────┘ │
│ ┌──────────────────────────────────────┐ │
│ │    回退到上一个正常版本               │ │
│ │ 恢复到 v3，之后的修改会保留可随时回来 │ │
│ └──────────────────────────────────────┘ │
│                                          │
│ 也可以直接输入你的想法…                   │
└──────────────────────────────────────────┘
```

卡片规范（强制）：

1. **每次必选项**：问题描述一句话（说清"发生了什么、影响到什么"）+ 选项列表，选项永远 1～3 个，不允许出现"只有文字没有选项"的问题通知。
2. **必有推荐**：恰好一个选项标记 `★ 推荐`，并附一句**推荐理由**（说明为什么是当前最优，如成功率、耗时、风险），推荐逻辑由 Orchestrator 根据问题类型、尝试次数、历史 Repair Memory 命中率和风险等级确定性生成（规则表驱动，不由模型自由发挥）。
3. **每个选项说清后果**：选项下方一句大白话说明"选了之后会发生什么、代价是什么"，让用户闭眼也能选。
4. **可自由输入兜底**：卡片底部保留"也可以直接输入你的想法…"入口，用户打字 = 视为第 4 种选项交给 Planner 处理。
5. **选择即继续**：点选后卡片折叠为一行系统操作条（"你选择了：呼叫人工协助"），流程立即继续，不需要再点发送。
6. **与右栏联动**：右栏卡点行动区同步高亮同一个推荐选项；点击右栏按钮 = 点击卡片上的对应选项，两处状态永远一致（同一事件源）。
7. **低风险推荐可自动执行**（可选策略，见 C11）：对"重试一次"这类无风险推荐，卡片可带"10 秒后不选择将自动执行推荐方案"倒计时，用户随时点其他选项或「先等等」中断；涉及回退、发布、权限的推荐**永不自动执行**。

推荐方案的生成规则示例：

| 场景 | 推荐选项 | 推荐理由（文案依据） |
|---|---|---|
| 首次构建失败 | 让 AI 再试一次 | 同类问题自动修复成功率 90%+ |
| 同一问题已失败 3 次 | 呼叫人工协助 | 继续自动尝试成功率低，人工最快 |
| 数据源不可用 | 重新选择数据源 | 不解决数据源，重试无意义 |
| 发布前安全扫描高风险 | 呼叫人工 / 转交审批 | 高风险不允许自动绕过 |
| 需求信息不足 | 回答澄清问题 | 补充信息后一次通过率最高 |

---

## 5. 核心交互流程

### 5.1 F1 新建大屏

```text
首页点「＋新建大屏」
  → 进入工作台（预览区为占位引导态）
  → 用户在对话区输入："帮我做一个服务器监控大屏，深色科技风"
  → 右栏展开，阶段时间线开始流动
  → 如需澄清：左栏出现澄清卡片（例：监控哪些指标？○ CPU/内存 ○ 网络 ○ 自定义）
  → 用户点选回答，流程继续（不打断预览区）
  → PREVIEW_READY：预览淡入 + 桌面通知"你的大屏做好了"
  → 用户继续对话修改，或点「发布」
```

设计决策：澄清问题**在对话流中以卡片呈现**，不打断、不弹窗；用户不回答时流程暂停等待（AWAITING_CLARIFICATION），行动区常驻提醒。

### 5.2 F2 持续对话修改

```text
用户："把 CPU 趋势图放大一点，告警列表挪到右下角"
  → Agent 回复修改计划（一句话 + 涉及 2 处调整）
  → 阶段时间线走"精简流程"（修改 → 构建 → 检查），不再展示完整阶段
  → 预览遮罩"正在生成新版本…" → 淡入新版本
  → 版本时间线自动新增节点 v4
```

增量修改的阶段展示**收敛为 3 步**，与新建（6～8 步）区分，避免每次小改都显得"很重"。

### 5.3 F3 版本管理与回退

顶栏 `↩版本` 打开右侧抽屉（覆盖执行面板位置）：

```text
┌─ 版本历史 ─────────────┐
│ ● v4  当前              │
│   "放大 CPU 图"  刚 才   │
│ ○ v3                   │
│   "增加集群筛选"  1小时前 │
│ ○ v2  ★已发布           │
│   "初版完成"      昨天    │
├────────────────────────┤
│ [预览此版本] [回退到此版本] │
└────────────────────────┘
```

- 每个节点：版本号 + 一句话变更摘要 + 缩略截图（hover 显示）。
- 「预览此版本」：预览区临时切换（顶栏出现"正在查看历史版本 v2"横幅 + 「返回当前」）。
- 「回退到此版本」：二次确认（"会保留 v2 之后的记录，随时可以回来"）→ 生成一个**新节点** v5（内容 = v2），不删除历史——小白不需要理解 Git，只理解"多了一张卡片"。

### 5.4 F4 卡点处理（观测性 §5.3 的产品化）

卡点发生时，**对话区立即生成问题处理卡片**（规范见 §4.3，必选项 + 必推荐）；右栏执行面板顶部的卡点行动区同步显示同一组选项（同一事件源，两处一致），按状态渲染：

| 卡点 | 行动区内容 |
|---|---|
| 等待澄清 | "需要你补充一点信息" + [去回答]（点击滚动定位到澄清卡片） |
| 外部阻塞（如数据源不可用） | 大白话原因 + [重新选择数据源] [呼叫人工] |
| 修复超预算升级 | "这个问题需要人工处理" + [呼叫人工] [先回退到上个可用版本] |
| 失败 | [重试一次] [回退到上个版本] [呼叫人工] |
| 停留超时（stall） | "比预期久了一点，仍在处理" + [继续等待] [重试] |

规则：选项**永远 1～3 个**；恰好一个 `★ 推荐` 选项高亮并附推荐理由；对话卡片与右栏行动区点击等效，状态实时同步。

### 5.5 F5 呼叫人工与协助过程

```text
用户点「呼叫人工协助」
  → 填写（可选）：一句话描述遇到什么问题
  → 系统操作条："已通知支持人员，预计 X 分钟内响应，你可以先做别的"
  → 客服在 Assist Console 接单 → 用户端执行面板出现：
      ┌──────────────────────────────┐
      │ 🎧 支持人员 小李 正在协助你     │
      │ 小李 正在查看执行过程…         │
      │ 小李 帮你重试了「表格超出边界」✓ │
      │ [结束协助]                    │
      └──────────────────────────────┘
```

- 客服的**每一个代办动作都实时推送**到用户端（透明原则），用户可随时「结束协助」收回控制权。
- 协助结束自动生成小结（做了什么、问题是否解决），进入对话流。

### 5.6 F6 发布

- 非管理员用户的「发布」= 提交发布申请：确认弹窗（截图预览 + 变更摘要 + 大屏名称）→ 进入审批，执行面板显示"等待审批"。
- 审批通过后对话流通知 + 桌面通知，版本节点打 ★ 标。

---

## 6. 设置中心 · 模型设置

从顶部 `⚙` 进入，模态整页（不新开窗口）：

```text
┌─ 设置 / 模型 ────────────────────────────────┐
│  当前使用：公司内置模型  ✓ 连接正常            │
│  ───────────────────────────────────────    │
│  服务商     [ 公司内置 ▾ ]                    │
│  API 地址   [ https://llm.internal…  ]       │
│  API Key    [ ●●●●●●●●●●  ]  显示            │
│  模型       [ qwen2.5-72b-instruct ▾ ]       │
│                                             │
│  [ 测试连接 ]                                │
│                                             │
│  高级 ▸（按角色指定模型：规划 / 编码 / 视觉）    │
└──────────────────────────────────────────────┘
```

「测试连接」反馈用大白话（调用 `/probe`）：

- ✅ "连接成功，支持图片理解，所有功能可用"
- ⚠ "连接成功。当前模型不支持看图片，布局检查将改用结构化检测，设计稿上传不可用"
- ❌ "连不上：地址似乎不对 / Key 无效（错误细节收在「查看详情」）"

原则：小白只需填 4 个字段；多 Provider 管理、路由策略收在管理员视图，普通用户不可见。

---

## 7. 状态、空态与异常处理

### 7.1 全局状态与可用操作矩阵

| 工作台状态 | 对话输入 | 预览操作 | 版本回退 | 发布 |
|---|---|---|---|---|
| 空闲（待输入） | ✅ | ✅ | ✅ | 有可用版本时 ✅ |
| 生成中 | ✅（排队） | 仅查看旧版 | ❌ | ❌ |
| 等待澄清 | ✅ | 仅查看旧版 | ✅ | ❌ |
| 卡点（阻塞/升级/失败） | ✅ | 仅查看旧版 | ✅ | ❌ |
| 人工协助中 | ✅ | ✅ | ❌（由客服操作） | ❌ |

### 7.2 多模态模式差异（交互层）

| 能力 | 多模态模式 | 纯文本模式 |
|---|---|---|
| 附件按钮 📎 | 可用（可传设计稿/截图） | 置灰，hover 提示"当前模型不支持图片，可在设置中更换模型" |
| 视觉检查提示 | 无感 | 首次生成时提示一次："当前为纯文本模式，布局检查使用结构化检测"（不再重复打扰） |
| 修复细节展示 | 截图 + VLM 结论 | 截图 + 结构化检测结论，文案同样大白话 |

### 7.3 断线与重连

- SSE 断连：顶栏出现黄色细条"连接中断，正在重连…"，预览与历史内容可正常查看，输入可用（本地暂存）。
- 重连成功：按 `seq` 补齐事件，细条消失；若期间任务完成，直接淡入最新预览 + 补发通知。

### 7.4 桌面通知（系统级）

仅在以下时机推送（克制）：生成完成、卡点需要操作、人工协助接入、审批结果。通知点击直达工作台对应位置。

---

## 8. 窗口与适配

- 客户端兼容 **Windows 10+ / macOS 12+ / Linux（Ubuntu 20.04+ 等主流发行版）**，三端同一套交互，不允许出现某端独有功能或某端缺失入口。
- 默认窗口 1440×900，最小 1200×720（小于最小值时右栏自动折叠为窄条）。
- 窗口装饰采用**原生标题栏**（不用自定义标题栏）：Windows / Linux 右上角最小化-最大化-关闭，macOS 左上角红绿灯，符合各平台用户习惯，适配成本最低。
- 快捷键按平台映射：Windows / Linux 用 `Ctrl`，macOS 用 `Cmd`（如发送 `Ctrl/Cmd + Enter`）；设置中提供快捷键一览表。
- 桌面通知走各平台通知中心；Linux 精简桌面无通知服务时，以应用内消息中心兜底（交互不变）。
- 三栏宽度可调（拖拽分隔条），布局记忆到本地。
- 全屏演示模式：纯大屏 + 按 `Esc` 退出，供投屏汇报。
- 分辨率切换仅影响预览缩放展示，截图与视觉验证始终以 1920×1080 逻辑分辨率为基准；大屏字体内嵌在产物中，保证三端预览与截图观感一致。

---

## 9. 客服视图（角色：客服/交付）

入口：首页左侧导航「协助队列」（仅该角色可见）。

```text
┌──────────────┬─────────────────────────────────────────────┐
│ 协助队列      │  现场回放（RUN-001 / 张三 / K8s 监控大屏）    │
│ 🔴 等待 12分  │ ──────────────────────────────────────────  │
│  张三·K8s大屏 │  [用户视角] [技术视角] 切换                   │
│ 🟡 等待 3分   │                                             │
│  李四·销售日报 │  状态机视图：PLANNING ✓ → … → REPAIRING ●   │
│ 🟢 已接入     │  （当前节点高亮 + 各节点停留时长）             │
│  王五·物流    │                                             │
│              │  事件时间线（可跳截图/日志/Diff/Trace）        │
│              │                                             │
│              │  代办动作：                                  │
│              │  [代答澄清] [重试问题] [回退版本]              │
│              │  [代提交审批] [接管编辑] [转交研发]            │
└──────────────┴─────────────────────────────────────────────┘
```

- 默认打开**用户视角**（与求助用户看到的完全一致，"客服无需用户截图即可还原现场"）；技术视角 = 完整事件原文 + 状态机 + OTel Trace 跳转。
- 代办动作全部需填写一句话原因（进审计 + 推送给用户）。

---

## 10. 待确认清单

请确认或拍板以下决策点，确认后进入客户端工程骨架开发：

| # | 决策点 | 本文建议 |
|---|---|---|
| C1 | 整体形态：三栏一屏式（对话 / 预览 / 执行面板） | 推荐，主流程不跳页 |
| C2 | 生成中对话输入不锁定（消息排队） | 推荐，符合小白直觉 |
| C3 | 回退 = 生成新版本节点而非删除历史 | 推荐，零理解成本且永不丢数据 |
| C4 | 澄清用对话内卡片，不用弹窗 | 推荐 |
| C5 | 预览区组件点选代入对话（"指哪改哪"） | 推荐，作为二期增强也可 |
| C6 | 发布对小白收敛为"提交申请"，审批在管理视图 | 推荐 |
| C7 | 语音输入 🎤 是否一期要做 | 建议二期 |
| C8 | 右栏执行面板默认展开还是折叠 | 建议：空闲折叠、任务中自动展开 |
| C9 | 客服视图内嵌同一客户端（按角色显隐） | 推荐，符合"不做 Web"前提 |
| C10 | 多模态缺失时附件按钮置灰 + 引导换模型 | 推荐 |
| C11 | 低风险推荐方案是否允许"倒计时自动执行"（如 10 秒后自动重试，可随时中断） | 建议一期开启，仅覆盖"重试"类无风险动作；回退/发布/权限类永不自动执行 |
| C12 | 推荐理由由规则表确定性生成（按问题类型/尝试次数/Repair Memory 命中率），不让模型自由发挥 | 强烈推荐，保证推荐理由可信、可审计 |
| C13 | 三端（Windows/macOS/Linux）窗口装饰用原生标题栏、快捷键按平台映射（Ctrl/Cmd） | 推荐，符合各平台习惯且适配成本最低；如要完全统一外观可改自定义标题栏，但需额外处理 macOS 红绿灯区域 |
